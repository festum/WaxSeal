i-am-looking-to-zany-dragon.md

# Build toolchains (fetched by `make deps`; pinned, not committed)
/.toolchains/
build/js/node_modules/

# Local build scratch
*.o
*.tmp

# Cross-compiled release binaries (`make release`)
/dist/

# Local diff scratch
*.patch

# NOTE: internal/jsassets/qjs.wasm and bg_bundle.js ARE committed (build outputs
# treated like vendored deps); do not ignore them.
