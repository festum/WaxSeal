# Node deps for rebuilding the browser bundle (`make deps`)
build/js/node_modules/

# Cross-compiled release binaries (`make release`)
/dist/

# Local scratch
*.tmp
*.patch

# NOTE: internal/browser/bg_browser_bundle.js IS committed (a build output treated
# like a vendored dep); do not ignore it.
