{
  "name": "waxseal-bgbundle",
  "version": "0.0.0",
  "private": true,
  "description": "WaxSeal BotGuard JS bundle build (esbuild: bgutils-js + shim + entrypoint)",
  "type": "module",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "bgutils-js": "^3.2.0"
  }
}
